{
  "name": "lunaclock",
  "private": true,
  "description": "LunaClock — static flip clock; Playwright smoke tests",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
